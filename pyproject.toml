[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "smart-home"
version = "0.1.0"
description = "Smart home API and scene schema"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115,<1",
    "pydantic>=2.10,<3",
    "pydantic-settings>=2.7,<3",
    "SQLAlchemy>=2.0,<3",
    "alembic>=1.14,<2",
    "psycopg[binary]>=3.2,<4",
    "argon2-cffi>=23.1,<26",
    "PyJWT>=2.10,<3",
    "email-validator>=2.2,<3",
    "uvicorn>=0.34,<1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3,<10",
    "httpx>=0.28,<1",
    "ruff>=0.9,<1",
    "jsonschema>=4.23,<5",
]

[tool.setuptools.packages.find]
where = ["apps/api/src", "packages/scene-schema/src"]
namespaces = false

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["apps/api/src", "packages/scene-schema/src"]
cache_dir = ".local/pytest_cache"

[tool.ruff]
target-version = "py312"
line-length = 88
cache-dir = ".local/ruff_cache"

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.ruff.format]
exclude = ["*.md"]
